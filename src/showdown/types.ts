export interface ShowdownState {
  encounterId?: string;
  monsterName?: string;
  encounterName?: string;
}

export interface Encounter {
  id: string;
  monsterName: string;
  encounterName: string;
}
